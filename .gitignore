__pycache__/
*.pyc
.venv/
dist/
*.egg-info

# Tokenizer vocabulary — large binary files
tokenizer/*.model
tokenizer/*.vocab
tokenizer-data/

# Corpus data — large, local only
*.jsonl
data/
# models/ stays ignored, but provenance records (story 131.24) are tracked.
**/models/**
!**/models/**/
!**/models/**/*.provenance.json
!**/models/**/provenance.json

.env
.DS_Store
.vscode/
